# Local data directory

No raw, processed, or patient-derived data belongs in Git. Configure an external
data root with `CARDIOSENTINEL_DATA_ROOT`; this directory is only a local default
for development. Dataset acquisition, access terms, checksums, and annotation
provenance must be recorded before use.

