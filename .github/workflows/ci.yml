name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install development, signal, ML, and neural dependencies
        run: python -m pip install -e ".[dev,signal,ml,neural]"
      - name: Lint
        run: python -m ruff check .
      - name: Test
        run: python -m pytest -q
