extends: base

project:
  profile: experiment-example

# Set sampling frequency only from verified record metadata or an explicitly
# documented dataset contract. Do not inherit a legacy 250 Hz assumption.
data:
  sampling_frequency_hz: null

# Frozen Phase-3 benchmark geometry; alternatives require a versioned protocol.
windowing:
  length_seconds: 10
  stride_seconds: 5
